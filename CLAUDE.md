# SMILE_JO — مولّد الصور الإعلانية

> هاد الملف بينقرأ تلقائياً بكل جلسة. فيه كل إشي لازم تعرفه عن المشروع
> والهوية وطريقة الشغل، عشان المستخدم ما يضطر يعيد الشرح.

---

## عن المستخدم

- **بهاء عويس** — صاحب براند **SMILE_JO** (عيادة/خدمات أسنان بالأردن).
- **مش مبرمج.** ما تشرحله كود ولا تطلب منه يعدّل ملفات إلا إذا هو طلب.
  اشتغل إنت وعطيه النتيجة جاهزة.
- **اللغة: عربي** — لهجة أردنية/شامية بسيطة. المصطلحات التقنية بالإنجليزي عادي.
- بيستعمل الصور للإنستغرام (ستوري / ريلز / بوستات).

---

## الهوية البصرية

| العنصر | القيمة |
|---|---|
| الأزرق الأساسي | `#014FC9` |
| الأصفر (ثانوي) | `#F9C03C` |
| الغامق | `#23232A` |
| الأبيض | `#F7F7F7` |

**الشعار:** مثلث أزرق مقلوب + رمز `JS` + كلمة `SMILE_JO` جواه.

| الملف | الاستعمال |
|---|---|
| `assets/logo.png` | أبيض+أزرق، خلفية شفافة — للخلفيات الغامقة |
| `assets/logo_dark.png` | نفس الرسمة بالغامق — للخلفيات الفاتحة |
| `assets/logo_raw.jpg` | الأصل قبل تفريغ الخلفية |

الكود بيختار النسخة لحاله حسب سطوع الخلفية.

---

## الشخصيات والمنتجات

| الملف | شو فيه |
|---|---|
| `assets/rose.png` | **روز** — الموديل الأساسي. شعر بني غامق طويل مموّج على كتفها اليسار، بشرة زيتونية، حواجب كثيفة، عيون بنية فاتحة. بليزر كريمي فوق قميص ساتان عاجي. |
| `assets/retainer_case.png` | **علبة الفنير المتحرك** — علبة بلاستيك صفرا لمّاعة، نقطتين مفصلات سود فوق، ستيكر أبيض دائري عليه شعار SMILE_JO. |

---

## خريطة الملفات

| الملف | الدور |
|---|---|
| `config.py` | **كل الإعدادات** — الصور، الأوصاف، الوضعية، الهوية، المقاسات |
| `prompt.py` | **يجهّز البرومبت للصق بـ ChatGPT** |
| `finish.py` | **شعار + مقاسات الإنستا بعد التوليد** |
| `prompts.py` | يبني نص البرومبت + بلوك الواقعية `REALISM` |
| `branding.py` | يركّب الشعار + الجملة + الاسم |
| `arabic_text.py` | يوصّل الحروف العربية ويقلب الاتجاه للكتابة على الصورة |
| `upscale_image.py` | تكبير وشحذ حدّة |
| `cutout_logo.py` | يفرّغ خلفية أي شعار جديد |
| `generate_rose_image.py` | توليد عبر API — **غير مستعمل** (محتفظين فيه لو صار معه مفتاح) |
| `output/` | الملفات الجاهزة |

---

## الإعدادات الحالية

```python
PERSON_IMAGE  = "assets/rose.png"
PRODUCT_IMAGE = "assets/retainer_case.png"
ASPECT_RATIO  = "9:16"      OUTPUT_SIZES = ["story", "4k"]      CROP_ANCHOR = 0.30
BRAND_ENABLED = True        BRAND_TEXT = ""       BRAND_POSITION = "top-right"
BRAND_LOGO_SIZE = 0.22      BRAND_BAR = True

CAPTION_ENABLED = True      CAPTION_TEXT = "ابتسامتك تستاهل الأفضل"
CAPTION_NAME = "روز"        CAPTION_POSITION = "bottom-right"
```

**ستايلات جاهزة:** `main` (ماسكة المنتج جنب وجهها) · `closeup` (كلوز أب على
المنتج) · `social` (ستوري مع مساحة للكتابة) · `portrait` (بدون منتج) ·
`candid` (عفوية — ضحكة طبيعية وإضاءة شباك، بتترك الثلث السفلي للكتابة)

---

## ⚠️ طريقة الشغل — مهم جداً

**المستخدم بيولّد الصور على ChatGPT، مش عن طريق API.**
ما عنده `GEMINI_API_KEY` وما بيستعمل `generate_rose_image.py`.

يعني الكود دوره **قبل وبعد** التوليد:

```
  ١. python prompt.py     →  يجهّز البرومبت
  ٢. المستخدم يلصقه بـ ChatGPT مع الصورتين المرجعيتين
  ٣. ينزّل الصورة
  ٤. python finish.py الصورة.png  →  شعار + مقاسات الإنستا
```

**لا تقول "رح أولّدلك الصورة"** — ما في أداة توليد صور بالجلسة.
دورك: تعدّل الإعدادات، تجهّز البرومبت، وتظبّط الصورة بعد ما يجيبها.

---

## 🎯 لما يطلب صورة — اعمل هيك

### الخطوة ١: ترجم الطلب لإعدادات

| هو بيقول | إنت بتعدّل |
|---|---|
| "ماسكة العلبة" | `--style main` |
| "قريب على المنتج" / "كلوز أب" | `--style closeup` |
| "ستوري" / "ريل" | `--style social` + `ASPECT_RATIO="9:16"` |
| "بوست انستا" | `ASPECT_RATIO="4:5"` + `OUTPUT_SIZES=["post"]` |
| "بدون منتج" / "بورتريه" | `--style portrait` |
| "عفوية" / "طبيعية" / "مش مترتبة" | `--style candid` |
| "حطلي جملة" / "اكتب عليها" | `CAPTION_TEXT` + `CAPTION_NAME` |
| "منتج تاني" | `PRODUCT_IMAGE` + `PRODUCT_DESCRIPTION` |
| "بنت تانية" | `PERSON_IMAGE` + `PERSON_DESCRIPTION` |
| "خلفية غامقة/زرقا" | `SCENE` |
| لبس معيّن | `PERSON_DESCRIPTION` |
| "بدون شعار" | `finish.py --no-brand` |
| "الشعار عاليسار" | `BRAND_POSITION` |

### الخطوة ٢: عدّل `config.py` بنفسك

ما تسأله يعدّل — هو مش مبرمج. عدّل إنت وقوله شو غيّرت بسطر واحد.

### الخطوة ٣: جهّزله البرومبت

```bash
python prompt.py --style <STYLE>
```

**واعطيه إياه جاهز بالشات** — انسخ النص كامل برد منسّق عشان ينسخه بضغطة.
ذكّره يرفع الصورتين قبل ما يلصق.

### الخطوة ٤: لما يبعت الصورة اللي طلعت معه

```bash
python finish.py <الصورة>
```

وابعتله الملفات بـ `SendUserFile`.

**انتبه:** إذا الصورة أصلاً فيها كتابة أو شعار من ChatGPT، اسأله إذا بده
الشعار كمان أو `--no-brand` — عشان ما يتكرر.

### الخطوة ٥: كمّت وادفع

البرانش الحالي: `claude/smiley-joe-rose-code-e2xis4`

---

## قواعد ثابتة — لا تكسرها

1. **الشعار والكتابة بينحطوا بـ `finish.py` بعد التوليد، مش بالبرومبت.** موديلات الصور
   بتشوّه الشعارات والكتابة — وبتكتب العربي مقطّع ومقلوب. البرومبت أصلاً بيمنع
   الموديل يرسم أي شعار أو كتابة.
2. **بلوك `REALISM` بـ `prompts.py` ما بينشال.** هو اللي بيمنع "الوجه
   البلاستيكي": مسام، ملمس بشرة، ثبات الملامح، عدسة 85mm.
3. **ثبات الملامح أهم إشي عنده.** ممنوع الموديل ينحّف الوجه أو يصغّر الأنف أو
   يكبّر العيون. مذكور صراحة بالبرومبت — خليه.
4. **`BRAND_TEXT = ""`** — الشعار جواه الكلمة أصلاً، فما منكررها.
5. **ولّد ٣ نسخ على الأقل** — اليدين والشعارات بتطلع مشوّهة أحياناً.
6. **ما ترفع `.env`** — موجود بـ `.gitignore`.

---

## شغل ممكن يطلبه لاحقاً

- **شعار جديد:** `python cutout_logo.py <الملف> --dark-variant`
- **منتج جديد:** يبعت صورة المنتج → حطها بـ `assets/` وعدّل
  `PRODUCT_IMAGE` + `PRODUCT_DESCRIPTION`
- **مقاسات متعددة:** شغّل نفس الستايل بـ `--aspect 9:16` و `4:5` و `1:1`
- **ألوان الإنستا:** لسا ما بعت سكرين شوت من البروفايل. الألوان الحالية
  مستخرجة من ملف الشعار.

---

## سجل القرارات

| القرار | السبب |
|---|---|
| Gemini كمزوّد أساسي | بياخد صورتين مرجعيتين (شخص + منتج) بنفس الطلب |
| الشعار بـ Pillow بعد التوليد | ضمان شعار مظبوط وإملاء صحيح ١٠٠٪ |
| `config.py` منفصل | المستخدم مش مبرمج — ملف واحد واضح بيغيّره |
| لون الكتابة `auto` | صوره خلفياتها بيج فاتح، والأبيض كان بيختفي |
| نسختين للشعار | الشعار أبيض، بيختفي على الخلفيات الفاتحة |
| `UPSCALE_4K` تكبير بس | تكبير مقاس، مش تفاصيل جديدة — قلنالوه بصراحة |
